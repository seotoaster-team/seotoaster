<?php $this->headLink()->appendStylesheet($this->websiteUrl . 'system/css/reset.css'); ?>
<?php $this->headLink()->appendStylesheet($this->websiteUrl . 'system/css/seotoaster.css'); ?>
<?php echo $this->headLink(); ?>

<div>
	<a href="<?php echo $this->websiteUrl; ?>" title="<?php echo $this->translate('Are you lost?'); ?>">&larr; <?php echo $this->translate('back to'); ?>&nbsp;<?php echo $_SERVER['SERVER_NAME']; ?></a>
</div>

<?php if (count($this->messages)) : ?>
	<ul id="messages" class="container alert alert-error text-center w350px t20px">
		<?php foreach ($this->messages as $message) : ?>
			<li>
				<span class="icon-exclamation-sign icon16" style="margin-top: -3px;"></span>
				<?php echo $this->escape($message); ?>
			</li>
		<?php endforeach; ?>
	</ul>
<?php endif; ?>

<div class="seotoaster container go-form">
	<div class="header">
		<?php echo $this->translate('Welcome Back'); ?>
        <div class="fl-right">
            <?php if(isset($this->languages) && is_array($this->languages) && !empty($this->languages)): ?>
				<?php foreach($this->languages as $lang): ?>
					<a class="flag-button" title="<?php echo ($this->translate('Select ') . $lang['language'] . $this->translate(' language'));?> " href="#">
						<img alt="<?php echo $lang['language']; ?>" class="lang-select" data-lng="<?php echo $lang['name']; ?>" src="<?php echo $this->websiteUrl . $lang['flag']; ?>" border="0" />
					</a>&nbsp;
				<?php endforeach; ?>
			<?php endif; ?>
		</div>
	</div>

    <div class="grid_12">
		<form class="grid_12 mt20px mb20px" action="<?php echo $this->url(); ?>" method="post" >
			<fieldset class="mb20px">
				<dl class="Zend_Form">
					<?php
						echo $this->loginForm->email;
						echo $this->loginForm->password;
					?>
				</dl>
			</fieldset>
			<?php echo $this->loginForm->submit; ?>
			<a id="forgot-password" class="btn btn-link" href="<?php echo $this->websiteUrl; ?>login/retrieve/"><?php echo $this->translate('Forgot password?'); ?></a>
		</form>
	</div>
</div>


<script type="text/javascript" charset="utf-8">
<?php $this->jQuery()->onLoadCaptureStart(); ?>
	$('.lang-select').on('click', function(e) {
        e.preventDefault();
		$.post( $('#website_url').val() + 'language/', {lng: $(this).data('lng')}, function() {
			window.location.reload();
		})
	})
	//$('.textcentered').buttonset();
<?php $this->jQuery()->onLoadCaptureEnd(); ?>
</script>
